name: Notion Weekly Crawl

on:
  schedule:
    - cron: "0 15 * * 1"  # 매주 월요일 KST 자정 (UTC 15:00)
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r ai_server/requirements.txt

      - name: Run crawler
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: python ai_server/app/services/notion_crawler.py --group weekly

      - name: 크롤링 결과 커밋 & 푸시
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ai_server/data/notion_*.md
          if git diff --cached --quiet; then
            echo "변경사항 없음, 커밋 생략"
          else
            git commit -m "🔧 chore: 노션 크롤링 데이터 자동 업데이트 (weekly)"
            git push
          fi

      - name: 배포 서버 재인덱싱
        run: |
          curl -X POST "https://eco-knock-ai.isek-ai.org/documents/index-all" \
            -H "Content-Type: application/json" \
            -d '{
              "file_paths": [
                "data/notion_멘토링.md",
                "data/notion_econovation.md"
              ]
            }'
